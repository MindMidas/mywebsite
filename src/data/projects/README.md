# Project card data

Each project card has its own editable file in this folder.

Edit the short `description`, `technologies`, and four `details` entries to change the front and back of each card. `sourceHref` controls the GitHub badge. `websiteHref` adds a website badge.

The display order is controlled in `index.tsx`.
